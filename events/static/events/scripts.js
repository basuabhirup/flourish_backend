// const openEvent = (e) =>{
//     const eventId = e.target.closest(".event").dataset.eventId
//     window.location = `/event/${eventId}`
// }

const backToHome = () => {
    window.location = '/'
}